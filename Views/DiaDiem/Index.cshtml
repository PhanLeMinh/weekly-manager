@model IEnumerable<WeeklyScheduleManagement.Models.DiaDiem>

@{
    ViewData["Title"] = "Quản Lý Địa Điểm";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="row mb-4">
    <div class="col-md-8">
        <h2><i class="fas fa-map-marker-alt"></i> Quản Lý Địa Điểm</h2>
        <p class="text-muted">Danh sách các địa điểm làm việc</p>
    </div>
    <div class="col-md-4 text-end">
        <a href="/DiaDiem/Create" class="btn btn-create">
            <i class="fas fa-plus-circle"></i> Thêm Địa Điểm Mới
        </a>
    </div>
</div>

@if (TempData["SuccessMessage"] != null)
{
    <div class="alert alert-success alert-dismissible fade show" role="alert">
        <i class="fas fa-check-circle"></i> @TempData["SuccessMessage"]
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
    </div>
}

@if (TempData["ErrorMessage"] != null)
{
    <div class="alert alert-danger alert-dismissible fade show" role="alert">
        <i class="fas fa-exclamation-circle"></i> @TempData["ErrorMessage"]
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
    </div>
}

<div class="filter-section mb-4">
    <form method="get" asp-action="Index">
        <div class="row g-3">
            <div class="col-md-9">
                <input type="text" name="searchString" value="@ViewData["CurrentFilter"]" 
                       class="form-control" placeholder="🔍 Tìm kiếm theo tên địa điểm hoặc loại...">
            </div>
            <div class="col-md-3">
                <button type="submit" class="btn btn-primary w-100">
                    <i class="fas fa-search"></i> Tìm kiếm
                </button>
            </div>
        </div>
    </form>
</div>

@if (Model != null && Model.Any())
{
    <div class="row g-4">
        @foreach (var item in Model)
        {
            <div class="col-md-6 col-lg-4">
                <div class="location-card">
                    <div class="location-header">
                        <div class="location-icon">
                            @switch (item.LoaiDiaDiem)
                            {
                                case "PhongHoc":
                                    <i class="fas fa-chalkboard-teacher"></i>
                                    break;
                                case "HoiTruong":
                                    <i class="fas fa-building"></i>
                                    break;
                                case "PhongHop":
                                    <i class="fas fa-users"></i>
                                    break;
                                default:
                                    <i class="fas fa-map-marker-alt"></i>
                                    break;
                            }
                        </div>
                        <div>
                            <h5 class="location-name">@item.TenDiaDiem</h5>
                            <span class="location-type">
                                @switch (item.LoaiDiaDiem)
                                {
                                    case "PhongHoc":
                                        <span>Phòng Học</span>
                                        break;
                                    case "HoiTruong":
                                        <span>Hội Trường</span>
                                        break;
                                    case "PhongHop":
                                        <span>Phòng Họp</span>
                                        break;
                                    default:
                                        <span>@item.LoaiDiaDiem</span>
                                        break;
                                }
                            </span>
                        </div>
                    </div>

                    <div class="location-body">
                        @if (item.SucChua != null)
                        {
                            <div class="location-info">
                                <i class="fas fa-users"></i>
                                <span>Sức chứa: <strong>@item.SucChua người</strong></span>
                            </div>
                        }

                        @if (!string.IsNullOrEmpty(item.MoTa))
                        {
                            <div class="location-info">
                                <i class="fas fa-info-circle"></i>
                                <span>@item.MoTa</span>
                            </div>
                        }

                        <div class="location-info">
                            <i class="fas fa-calendar"></i>
                            <span>Ngày tạo: @item.NgayTao.ToString("dd/MM/yyyy")</span>
                        </div>

                        <div class="location-info">
                            @if (item.TrangThai)
                            {
                                <span class="badge bg-success">
                                    <i class="fas fa-check-circle"></i> Đang hoạt động
                                </span>
                            }
                            else
                            {
                                <span class="badge bg-secondary">
                                    <i class="fas fa-times-circle"></i> Ngừng hoạt động
                                </span>
                            }
                        </div>
                    </div>

                    <div class="location-footer">
                        <a href="/DiaDiem/Edit/@item.MaDiaDiem" class="btn btn-sm btn-warning">
                            <i class="fas fa-edit"></i> Sửa
                        </a>
                        <a href="/DiaDiem/Details/@item.MaDiaDiem" class="btn btn-sm btn-info">
                            <i class="fas fa-eye"></i> Chi tiết
                        </a>
                        <a href="/DiaDiem/Delete/@item.MaDiaDiem" class="btn btn-sm btn-danger"
                           onclick="return confirm('Bạn có chắc muốn xóa địa điểm này?')">
                            <i class="fas fa-trash"></i> Xóa
                        </a>
                    </div>
                </div>
            </div>
        }
    </div>
}
else
{
    <div class="empty-state">
        <i class="fas fa-map-marked-alt fa-4x text-muted mb-3"></i>
        <h4 class="text-muted">Chưa có địa điểm nào</h4>
        <p class="text-muted">Nhấn vào nút "Thêm Địa Điểm Mới" để tạo địa điểm đầu tiên</p>
        <a href="/DiaDiem/Create" class="btn btn-create mt-3">
            <i class="fas fa-plus-circle"></i> Thêm Ngay
        </a>
    </div>
}

<style>
    .btn-create {
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        color: white;
        border: none;
        padding: 12px 30px;
        border-radius: 25px;
        font-weight: 600;
        transition: transform 0.3s;
    }
    .btn-create:hover {
        transform: scale(1.05);
        color: white;
    }
    .filter-section {
        background: #f8f9fa;
        padding: 20px;
        border-radius: 15px;
    }
    .location-card {
        background: white;
        border-radius: 15px;
        box-shadow: 0 3px 10px rgba(0,0,0,0.1);
        overflow: hidden;
        transition: all 0.3s;
        height: 100%;
        display: flex;
        flex-direction: column;
    }
    .location-card:hover {
        transform: translateY(-5px);
        box-shadow: 0 5px 20px rgba(0,0,0,0.15);
    }
    .location-header {
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        color: white;
        padding: 20px;
        display: flex;
        align-items: center;
        gap: 15px;
    }
    .location-icon {
        width: 50px;
        height: 50px;
        background: rgba(255,255,255,0.2);
        border-radius: 10px;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 1.5rem;
    }
    .location-name {
        margin: 0;
        font-size: 1.2rem;
        font-weight: bold;
    }
    .location-type {
        font-size: 0.9rem;
        opacity: 0.9;
    }
    .location-body {
        padding: 20px;
        flex: 1;
    }
    .location-info {
        display: flex;
        align-items: center;
        gap: 10px;
        padding: 8px 0;
        font-size: 0.95rem;
    }
    .location-info i {
        color: #667eea;
        width: 20px;
    }
    .location-footer {
        padding: 15px 20px;
        background: #f8f9fa;
        display: flex;
        gap: 10px;
        justify-content: center;
    }
    .empty-state {
        text-align: center;
        padding: 80px 20px;
        background: #f8f9fa;
        border-radius: 15px;
    }
</style>